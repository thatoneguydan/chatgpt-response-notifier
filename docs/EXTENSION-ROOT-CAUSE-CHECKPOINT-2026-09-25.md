# Extension bug checkpoint — 2026-09-25

Canonical baseline: `main` at `36f1a4fd64600fa92cb2cba3ff4ee1b627d3fb72` (notifier 0.9.81, Quick Continue 1.2.18). Work branch: `fix/extension-ui-notify-timer-20260925`. This document records source findings and separates verified behavior from live-machine hypotheses.

## Findings from source

1. The notifier's `attachment-script.js` creates a canonical countdown button and updates it every second. A second runtime (`quick-continue-status-owner-v6.js`) creates another countdown button and hides the first every second; `quick-continue-status-stabilizer.js` also hides the first, and `quick-continue-monitor-bridge.js` mirrors the toolbar and may restore its display. These independent DOM owners and document-wide mutation observers form a rendering feedback loop. Source explains repeated geometry changes; the exact visual cadence still needs a browser reproduction.
2. Quick Continue's `writeContentEditable()` and manual timestamp `replaceContentEditable()` construct a `<br>` for every `\n`, then dispatch a synthetic `input` event with the entire string. `composerText()` collapses all whitespace before it verifies the write. Thus a structurally different multiline composer can pass verification and be sent. Fix requires reading and comparing exact paragraph/newline structure and one input path, tested against the current contenteditable editor and text controls.
3. Three separate scripts arm the watchdog on a trusted Quick Continue action (`attachment-script.js`, `quick-continue-monitor-bridge.js`, `watchdog-page-authority-v3.js`), in addition to network request observation. This can re-arm after a definitive status is parked. `watchdog-request-lifecycle-fix-background.js` has an in-memory terminal latch, but it does not make all subsequent arming paths conditional on the same prompt/request identity.
4. The stop button can use the persisted watchdog record's stopped state with an `operator-timer-stop` reason while retaining enrollment. A subsequent real request must rearm with a fresh request identity. Pausing Monitor would violate the requested behavior.
5. A Windows notification and chime are coupled at `ToastManager.Show()`; the chime plays only when a toast is newly presented. The helper may accept a request without presenting (`response-already-accepted`, `response-already-open`, `dismissed-tombstone`). The extension outbox currently acknowledges `toast.accepted` without checking `presented`; this can remove a queued notification even if it was not visible. The exact reason for the user's current total silence requires helper diagnostics from Glass or a reproducible source regression. Avoid resetting dedupe stores or replaying old toasts blindly.
6. The bounded Glass runtime artifact from workflow run `36148590064` (collected 10:36 AM EDT, notifier 0.9.80) shows a connected helper and loaded extension. Its recent delivery tail contains two `response-stream-stream-read-error` events, a `request-error-status-probe-no-terminal-code`, and no toast presentation in that tail. The 124 recent `toast-dismiss-conversation-applied` entries each removed zero toasts. This does not prove that a coded completion was missed, but locates observed silence upstream of sound playback. The earlier artifact from run `36130140195` also shows repeated empty dismissals. `content-script.js` sends `CHATGPT_PAGE_RETURNED` on **every** focus, visibility, pointerdown and keydown, creating unnecessary helper messages and diagnostic churn. Replace this with a true return/interaction transition and preserve manual dismissal behavior.
7. The DOM completion path in `service-worker.js` requires an exact normalized match between upstream response text and rendered assistant text before it will notify. Real rendering differences can make that comparison fail without a diagnostic. The stream path is independent but stream read errors are present in the Glass evidence. A robust identity-bound fallback should log why a coded completion was rejected and should not invent completion on a mere request error.

## Work sequence

- Collapse timer presentation to one notifier-owned renderer and remove redundant DOM mirroring/hiding. Preserve one control for Monitor and one countdown row with separate Reset and Stop actions.
- Preserve exact newline semantics through composer insertion and verification. Test `\n`, adjacent breaks, blank lines and trailing breaks for both toolbar and manual timestamps.
- Make watchdog definitive stop authoritative for `PLANNING_ACTIVE`, `COMPLETE_APPLIED`, `COMPLETE_NO_CHANGES` and `BLOCKED_HUMAN`; prevent late same-turn arms and ensure next real request re-arms after an operator timer stop.
- Trace notification queue/host acknowledgment, add a regression for an accepted-but-unpresented notification and a bounded diagnostic for a live failure; verify helper behavior before claiming Windows notifications fixed.
- Run the complete repository suite and a focused browser DOM exercise; audit adjacent races and package/release only after gates pass.

No live Glass Chrome or Windows helper process is accessible from this execution environment. Source tests cannot establish actual Windows presentation or sound playback.

## Implementation checkpoint 1

Local source on this branch now removes the extra countdown owner, stabilizer injection and toolbar display mirroring. `quick-continue-status-owner-v6.js` now runs as runtime 7 and owns the one countdown row; it uses full toolbar width, black right-aligned wrapping text, a Reset control, and a revision-bound Stop control. The worker parks an operator-stopped watchdog without disabling Monitor, ignores late arms for the same prompt, and re-arms for a later request. `content-script.js` reports a page return only on an actual inactive-to-active transition.

Quick Continue uses a shared `composer-text.js` editor transaction for both toolbar sends and manual timestamps and checks exact newline text before allowing a send. The new file is included in the manifest, open-tab reinjection, PowerShell install, managed ZIP release, validation, and the C# bundle validator. This explicit-file-list discovery is itself an additional update bug that otherwise would have made the new code fail after update.

The local extension + standalone suite passes **484 of 484** tests (`node --test tests/extension/*.test.mjs tests/standalone-quick-continue.behavior.test.mjs`), including new exact-newline and operator Stop/rearm checks. The VM editor test cannot prove ChatGPT's current Lexical DOM serializes multiline `execCommand('insertText')` exactly; a real browser run remains needed. This code has not yet been packaged or observed on Glass. The notification silence remains unresolved; the outbox already retries missing helper acknowledgments in `delivery-reliability-background.js`, so the observed stream read errors and completion binding need further investigation before claiming a fix.

## Implementation checkpoint 2 and further audit

- A late request-start callback supplied an empty prompt key to the watchdog reconciliation path. It could reopen a stopped timer for the same request, which explains why terminal stops could appear inconsistent. The stopped record now retains that request until a later request or a genuinely new prompt appears. A test covers all four definitive statuses, their deadline clearing, and a late empty-key request-start.
- Quick Continue's document-wide observer treated the countdown button's per-second text changes as page mutations and scheduled a full toolbar sync on every tick. The timer node is now classified as notifier-only churn. Manual timestamp selection also skips a hidden stale composer, matching the primary Quick Continue send path.
- The Windows outbox already retries missing acknowledgments. `TEST_NATIVE_TOAST` returned success when a WebSocket send succeeded, even if the helper never presented a window or played a chime; it now waits for `toast.accepted` with matching notification ID and `presented:true`. A cloned response stream read error now launches a bounded rendered-status recovery only when the current request ID, conversation, prompt, assistant, and status code all match the page observation; an old status cannot authorize a toast. This recovery is tested with matching and mismatched request identities. **The current live Glass artifact has no coded completion in its bounded tail**, so it cannot prove the user's actual missed toast is fixed. A controlled coded completion and test toast on the updated Windows machine are still required.
- The candidate versions are notifier **0.9.82** and Quick Continue **1.2.19**. The Quick managed-update feed remains at 1.2.18 until its release workflow builds the archive and updates the feed on main. Do not hand-edit the feed's SHA or digest. The prior source checkpoint is GitHub commit `2ecf1baeefc76ee2e7839fc4b71ae96bbd538eaa`; new changes after that commit must be synced separately.
- A separate deep audit found a contenteditable that is empty except for placeholder line breaks could be mistaken for a draft. Availability now checks for meaningful text while exact post-insertion verification still retains every newline. A delayed terminal stop request carrying the previous prompt key is rejected after a new prompt becomes current in both worker stop routes. Missing DOM status, failed response binding and missing request ownership now leave bounded diagnostic reasons rather than disappearing silently.
- The obsolete `quick-continue-status-fallback.js` and `quick-continue-status-stabilizer.js` files had no remaining production imports and were removed from source. The new owner disposes the previous runtime on an already-open page during hot injection.

The full JavaScript test discovery passes **489 of 489** (`node --test $(rg --files tests | rg '\.test\.mjs$')`); every extension and Quick Continue JavaScript file also passes `node --check`, and `git diff --check` is clean. This is source validation, not Windows acceptance.

Remaining deterministic sequence: run the complete local test suite and source checks, sync the updated branch, open a PR against main, inspect the Windows validation run, then merge if it passes and inspect both release workflows. Live acceptance needs a controlled response ending in each definitive footer, an exact multiline prompt, a Stop/rearm interaction, and one helper-confirmed test toast on Glass; compare new bounded diagnostics to the above artifact. Do not claim notification or sound presentation solely from source tests.
